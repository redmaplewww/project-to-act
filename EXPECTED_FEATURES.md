# 云湃项目预期功能总表

> 来源：`yunpai-project-to-act-20260827.zip` 中五份 `.project-to-act` 文档。
> 整理日期：2026-08-27。
> 本表按后续决定覆盖早期决定、去除重复证据和实施过程，只保留产品最终需要具备的功能。状态不等于生产验收结果。

## 状态说明

- **已实现/已部署**：已有实现或环境能力，但仍可能存在数据治理或现场验收缺口。
- **本地试点**：代码和测试候选已形成，尚未提交、部署或接入真实生产事实。
- **待实现**：需求或设计已确认，尚未形成完整实现。
- **待确认**：文档保留了方向，但仍有业务口径需要用户确定。

## 一、总体业务闭环

系统预期形成以下主流程：

1. M0 管理主数据、订单源文件、BOM、SOP 和工图。
2. M1 识别并确认客户订单，按产品明细生成生产订单。
3. M2 分别确认 BOM 和 SOP/工艺路线。
4. M3 按生产订单核算物料需求、库存、在途和采购缺口。
5. M4 生成人工复核的采购单及供应商邮件，并跟踪承诺交期和到货。
6. M7 完成送货登记、质检、入库、预占、正常领料和超领。
7. 生产订单全部物料真实齐套后，M5 确认排程输入并计算 PMC。
8. Agent 对话页展示全过程、业务制品、人工门、证据、状态和异常。

当前开发范围止于 **M5 生成并确认 PMC 候选**；PMC 后续的正式发布、派工和生产执行不属于本期。

## 二、M0：主数据与业务文档

1. **多格式历史文档预览**（已部署）
   - 支持 PDF、图片、Excel、CSV、DOCX，以及 JSON、TXT、LOG、Markdown、XML、YAML 等安全文本预览。
   - 缺文件、不支持格式、压缩包、DWG/OLE2 等情况准确降级为提示或下载。
   - 保持租户隔离、访问权限、安全响应头和资源访问控制。

2. **入库源文件与业务文档分离展示**（已部署，历史 tenant 治理待完成）
   - 在 M0 数据导入的主数据管理入口中，分别展示业务文档和原始源文件。
   - `import_document` 作为源文件权威，不重复创建主文档。
   - BOM、SOP 与其来源文档建立可追溯关联。

3. **订单关联 BOM、SOP、工图预览**（部分已实现）
   - Agent 对话页提供工图和 SOP 完整工作台跳转。
   - 后续按当前订单自动解析并免上传预览本次使用的 BOM、工图和 SOP。
   - 通过稳定 `order_id`、TaskID 和版本规则固定运行中及完成后的文档版本。
   - 纯预览不得修改 M1–M5 业务状态。

4. **三层料号与文档版本追溯**（待实现）
   - 保存客户料号、内部料号和 BOM 明细物料号。
   - 内部料号有专用规则时按规则生成；没有时使用对应 BOM 单号。
   - 产品明细关联内部料号、BOM 表号、SOP 表号和生产订单号。

## 三、M1：客户订单与生产订单

1. **客户订单头与多产品明细**（待实现）
   - 订单头保存客户订单号。
   - 一张客户订单可包含多款产品/线材，每个明细保存客户料号、数量、单价和行总价。
   - 保存明细总价和整单汇总总价；币种统一人民币，当前不处理税。

2. **生产订单生成与映射**（待实现）
   - 每个产品明细在 M1 确认时生成一个独立生产订单号。
   - 持久化客户订单号、产品明细和生产订单号的映射，供后续模块查询和追踪。
   - 同一客户订单中的不同产品独立进入 M2–M5，不要求同步推进。

3. **M1 人工确认门**（待实现）
   - 信息修改在确认前完成。
   - 未完成 M1 人工确认，不得进入 M2。
   - 确认人、角色权限和审计字段仍需形成最终后端规则。

## 四、M2：BOM 与 SOP/工艺路线

1. **BOM 与 SOP 双人工门**（待实现）
   - BOM 和 SOP 分别展示、分别确认。
   - 两项都确认完成后才允许进入 M3。
   - 当前先实现前端确认交互，具体专业人员、权限和审批模型待确定。

2. **版本化工程包**（待实现）
   - 已批准 BOM 和完整 SOP 经工艺/IE 审核后形成版本化、可追溯的生产路线。
   - SOP 工序顺序是硬约束，不允许 PMC 自行移动、增加、删除或修改。
   - 只允许 SOP 明确声明的并行、条件和返工路径。

3. **真实订单工程资料闭环**（进行中）
   - W-H128 已形成订单专用 SOP、34 工位路线骨架和物料候选。
   - 正式生产仍需 BOM、路线、工程参数、工时、资源、日历、库存、测试和首件批准。

## 五、M3：物料需求、库存分流与预占

1. **按生产订单独立核算物料**（待实现）
   - 每个生产订单独立计算完整物料需求和缺口。
   - 库存满足时跳过 M4，直接等待进入 M5；不足时进入采购流程。

2. **库存使用优先级**（待实现）
   - 优先使用该生产订单的专用库存。
   - 其次使用公用库存。
   - 安全库存仅限紧急订单，并必须提交包含订单、物料和数量的独立人工审批。

3. **订单级原子预占**（已确认、待实现）
   - M3 发起预占，M7 作为唯一库存账原子执行。
   - 一个生产订单的本次预占全成或全败。
   - 区分库存批次的订单归属与实际预约明细。
   - M5 只读消费预约结果，不直接修改库存。

4. **在途采购核算**（待实现）
   - 供应商承诺数量和交期可计入 M3 的在途计算。
   - 在途足够覆盖缺料时只跟踪，不重复采购。
   - 在途仅覆盖部分缺料时，只采购剩余缺口。
   - 在途不是库存，不能放行 PMC。
   - 无生产订单归属的公用在途如何分配仍为待确认候选。

5. **真实齐套门**（待实现）
   - 只有真实到货、质检通过并正式入库的物料才能计入齐套。
   - 当前生产订单的整单 BOM 全部齐备后才能进入 PMC。
   - 首版不做逐工序原料投放或局部齐套开工。

## 六、M4：采购单、供应商通信与到货跟踪

1. **采购建议转采购单草稿**（pressure-only 链已部署，production 待真实数据）
   - M3 产生不可变采购建议，M4 拥有采购单实体。
   - 采购单绑定 TaskID、计划版本、revision 和 checksum，保证幂等。
   - 厂长可修改、批准、驳回或退回修改；关键修改产生新 revision。
   - 未批准不得进入邮件、ERP/SRM 或供应事实。

2. **按生产订单拆分采购**（待实现）
   - 不同生产订单即使物料相同也分别生成、确认、发送和跟踪。
   - 同一生产订单的一种缺料不得拆给多个供应商。
   - 多个供应商仅根据历史交期和守时履约信息给出推荐，由人工最终选择。

3. **供应商邮件双人工门**（record-only 已部署）
   - 只从有效的已批准采购单 revision 生成邮件。
   - 邮件包含确定性业务字段和来源，支持人工编辑及独立复核。
   - 状态链包括草稿、编辑、待审核、批准模拟、模拟留痕。
   - 当前首版只做 `record-only`，不得真实外发。

4. **承诺交期处理**（待实现）
   - 首次成功识别的供应商承诺交期自动进入 M3 在途计算。
   - 后续修改或特殊情况不得自动覆盖原交期，统一转人工。
   - 承诺交期不等于库存，不能放行 PMC。

5. **自动跟进与催交**（待实现）
   - 从采购邮件实际发送到承诺交期的时间区间过半时自动发送进度跟进。
   - 临近交期时自动催交，当前暂定为提前 3 天。
   - 这是首次邮件人工确认规则之后的自动化例外，参数后续可配置。

6. **部分到货跟踪**（待实现）
   - 已到部分进入后续送货/质检流程。
   - 剩余未到数量继续保留原生产订单归属并持续跟踪。

## 七、M7：送货、质检、库存和领料

1. **对话式仓储工作流**（Mock 原型已完成，真实集成待实现）
   - 使用独立状态机保持跨轮会话和幂等提交。
   - 支持真实只读适配器或可替换 Mock；原型写入独立临时库。
   - AI 无法确定时转人工，不猜测业务结果。

2. **送货单识别与人工校正**
   - 上传照片或文件后识别送货信息。
   - 支持确认、拒绝和修改识别结果。
   - 上传只表示到货/待质检，不直接形成库存。

3. **库存归属选择**
   - 送货登记选择生产订单号，质检通过后进入该订单专用库存。
   - 不选择生产订单号，质检通过后进入公用库存。
   - 当前生产订单号建议人工选择，但暂不作为强制门禁。

4. **质检通知与结果确认**
   - 确认送货信息后生成待检任务并通知品保。
   - 品保通过后正式入库；不通过时转人工处置。
   - 质检抽样和更复杂的处置规则属于后续增强能力。

5. **正常领料与超领**
   - 正常领料优先关联生产订单，无匹配订单时允许人工补录。
   - 领料时消费预约并真实扣减库存。
   - 支持超领订单选择、物料和数量录入，说明可选。
   - 标准用量额度、批次扣减和超领审批属于后续增强能力。

6. **订单取消人工处置**（当前不自动化）
   - 生产订单取消统一转人工。
   - 本期不自动执行预约释放、专用库存转公用、改绑或报废。

## 八、M5：PMC 有限产能排程

### 8.1 输入、数据模型与门禁

1. **生产订单级物料齐套门**（待实现）
   - 只有整单 BOM 真实库存齐套才能进入 PMC。
   - 在途、承诺交期、模拟回执和 pressure-only 数据不得放行 production PMC。

2. **排程输入人工确认门**（待实现）
   - 物料齐套后完整展示订单、路线、资源、日历、WIP、约束、默认值和来源。
   - 人工确认后生成不可变输入快照，再开始计算。
   - 缺失、冲突、默认、例外或非最优情况必须先给出结构化预览。

3. **统一排程对象模型**（本地试点）
   - 区分物理工位 `station`、SOP 路线步骤 `route step`、工位间缓冲边 `buffer edge` 和在制批 `WIP lot`。
   - BOM 用于整单齐套，不在首版映射到具体消耗工序。
   - 人员、机器、模具和治具使用具体资产 ID、状态、资格和兼容关系。

4. **流式生产模型**（本地试点）
   - 最终技术基线为 `STREAMING_FLOW`，覆盖早期“离散批次流”决定。
   - 每两个相邻工位之间默认建立生产订单专属 WIP。
   - WIP 按订单、产品、工位和物料隔离，不跨订单自动共享。
   - 初始真实 WIP 可以为 0，但必须显示缺料、饥饿或空闲原因，禁止制造在制事实。

### 8.2 资源与日历约束

5. **逐人逐机逐模治具排程**（本地试点）
   - 首版必须排到具体人员和具体资产，不只使用人数或技能池。
   - 人员可用时间、工位资格、个人速度、机器能力和占用时间进入模型。
   - 默认一人一机并整段占用人员；特殊配比必须来自已批准 SOP 或资料。
   - 多人同工位的产能不得超过可用机器数量和机器能力。

6. **组合速度与默认值治理**（待生产数据）
   - 支持产品、人员、机器组合速度。
   - 缺少特殊值时使用已确认默认值；冲突转人工。
   - 缺失良率当前默认 98%，但必须在输入预览中明确并经人工确认。

7. **人员日历、资格和跨工位复用**（本地试点）
   - 日历主要来自排班和考勤，人工输入优先，精确到小时；加班须预先批准。
   - 工位资格为硬约束。
   - 一人可顺序跨工位但不得时间重叠；复用工位序号差至少 6，即中间完整隔开 5 个工位。
   - 首版只允许同一流水线、同一产品路线内复用，不跨线。

8. **机器、模具和治具状态**（本地试点）
   - 每项资产独立编号，维护可用、故障、停用等可扩展状态。
   - 独占和兼容映射为硬约束；多个兼容资源由求解器择优。
   - 故障设备必须人工确认恢复后才能重新进入资源池。
   - 缺少正式资产 ID 的方案只能作为测试 PMC，不得晋升 production。

9. **班次、暂停与恢复**（待实现/部分试点）
   - 工序按有效工作时段累计，允许下班后跨日暂停和恢复。
   - 正常跨班继续使用原人员和原机器，并锁定模具/治具。
   - 机器故障保留已完成进度，只重排剩余工时。
   - 人员、机器、模具和治具首版均不跨生产线。

10. **换型时间**（待确认首版范围）
    - 按具体机器及前后产品/模具组合确定换型时间。
    - 换型期间占用机器和模具/治具；同模异品允许存在较小换型时间。
    - 无批准资料时默认 0，并在预览中说明。
    - 技术人员、提前换线任务、跨线搬运和寿命管理当前不进入求解器。

### 8.3 排程规则与优化目标

11. **SOP 顺序硬约束**
    - 严格遵循已发布路线的先后、并行、条件和返工关系。
    - 已完成工序固定，不允许重排。

12. **时间精度与最早开工**
    - 所有时间精确到分钟，非整数分钟向上取整。
    - 允许设置人工可修改的最早开工硬下限，不设置最晚开工边界。
    - 偏移按自然日计算，实际加工仍服从资源工作日历。

13. **交期硬约束与受控延期**
    - 正常阶段所有订单交期为硬约束。
    - 只有输入完整、已确认且求解器明确证明无可行解时，才允许生成延期候选。
    - 超时、未知、系统错误或资料缺失直接转人工，不生成延期方案。
    - 延期候选必须展示风险并经人工确认才可成为有效 PMC。

14. **订单优先级**
    - 优先级顺序为：人工紧急 > 人工优先 > 自动综合分数。
    - 同一人工层级再按自动分数排序。
    - 紧急订单在竞争资源上真实优先，同时提示其对其他订单交期的影响。
    - 人工改级需经过“允许重算”和“批准应用”两个人工门。

15. **分层优化目标**
    - 在硬约束全部满足后，依次考虑自动综合优先度、减少换型、避免 WIP 断料、提高人工利用率和保持计划稳定。
    - 人工利用率是软目标，只减少可避免空闲，不强迫无意义作业。
    - 自动优先度权重由人工配置和管理员确认，AI只能解释，不能自行修改。

16. **订单和批次规则**
    - 首版生产订单默认不可拆批。
    - 首版单个订单不得多线并行。
    - 同优先级订单不得为加速一个订单而推迟另一个订单的开工。
    - 满足全部硬约束时允许调整订单顺序。
    - 同模具订单尽量连续，减少换模次数。

17. **瓶颈与 WIP 管理**
    - 初期保留瓶颈字段，人工识别优先；自动识别和自动修正后置。
    - WIP 以可连续消耗时间或等价数量表达，按工位设置人工软目标。
    - 无目标时默认 0；低于目标时避免断料，超过目标只提示，不设置首版硬容量上限。
    - 增人、增机和替代线只生成建议，不自动创建资源或改变排程。
    - 瓶颈绕行和余量再分配属于后续能力。

### 8.4 求解、重排、版本和解释

18. **求解与独立校验**（本地试点）
    - 采用 CP-SAT 生成候选排程。
    - 使用确定性流式仿真回放 WIP、产消和工位状态。
    - 使用独立 validator 检查硬约束、库存/WIP 守恒和输出一致性。
    - 求解器未证明最优或出现复杂非核心边界时转人工。

19. **动态重排**（本地试点）
    - 人员缺勤、资格撤销、机器故障、进度或 WIP 更新可触发重排。
    - 运行进度、人员、机器和剩余工时写入日志数据库。
    - 首版进度与 WIP 来源为人工或文件确认，不接 MES。
    - 未开工工序可以重排，已完成工序保持冻结。

20. **双人工门应用机制**（本地试点）
    - 事件发生后先由人工允许重新计算。
    - 新候选展示完整差异、风险和影响，再由人工批准是否应用。
    - 系统可后台计算更优候选，但不得自动替换当前有效 PMC。

21. **不可变版本、回滚与三指针**（本地试点）
    - 保存候选版本、当前测试版本和当前生产版本，严格隔离。
    - 保存父版本、revision、checksum、结构化 diff、trace 和追加式应用历史。
    - 回滚必须按当前事实和硬约束重新验证；旧版可行才可恢复，不可行则生成新候选重新审批。
    - 测试版本和合成事实不得晋升生产版本。

22. **Agent 参数治理**（本地试点/范围待确认）
    - 在 Agent 对话中通过 `Resolve → Prepare → Commit` 修改参数。
    - 业务参数、阈值、权重、默认值和算法 profile 可版本化。
    - 参数修改、影响分析、候选失效、重算和回滚均需结构化预览。
    - 参数提交和 PMC 应用是两个独立人工门。
    - 身份权限、审计、SOP 顺序、资源不重叠、库存/WIP 守恒、真实库存准入及测试不得晋升 production 等不变量不可关闭。

23. **PMC 结果输出**（本地试点）
    - 按订单、产品、工序输出计划时间、工位、人员、机器、模具/治具和 WIP 状态。
    - 区分需求量、已完成量、未完成量、异常原因和资源缺口。
    - 输出来源、输入版本、算法 profile、约束结果、风险、diff、trace 和 checksum。
    - 支持多订单优先关系、资源变化和资格撤销结果的可解释回读。

## 九、Agent 对话产品形态

1. **Agent 对话作为唯一正式业务入口**（部分已部署）
   - 主流程保持对话形态，不改造成 Dashboard 式运营后台。
   - Dashboard、Worker、Leader 等页面不计入正式功能验收入口。

2. **M1–M5 业务卡片和聚焦工作区**（部分已部署）
   - 对话流中展示订单、BOM/SOP、库存齐套、采购、供应、PMC 和执行上下文卡片。
   - 复杂 PMC 内容采用“对话主线 + 结构化消息 + 同会话聚焦工作区”承载。
   - production 与 provisional/pressure-only 卡片严格分离。

3. **工作进度和活动流**（已部署，性能门待完成）
   - M1–M5 卡片可展开真实活动时间线。
   - Agent 正文与结构化工作动态同屏流式展示。
   - 支持对象、revision、当前步骤、总步骤和刷新/重连恢复。
   - 不展示隐藏推理，不伪造进度百分比，不增加额外 LLM 调用。

4. **统一人工交互**（部分已部署）
   - 所有业务门提供确认、修改、拒绝、返回和继续操作。
   - 人工兜底前先展示完整结构化预览。
   - 长内容卡片支持滚动、折叠和恢复，不因页面固定高度被裁切。

5. **全链路追踪和日志**（部分已部署）
   - 业务制品绑定 TaskID、run_id、revision、checksum 和来源。
   - 支持执行日志下载和按 TaskID 恢复流程。
   - 未知阶段明确显示 `unknown`，不得伪装为成功或自动推进。

6. **生产来源详情卡片**（已实现只读首版）
   - 显示工序、工位、计划、实际、人员绑定、资源 ID、来源时间、版本和可信等级。
   - 排除 simulation、pressure-only、mock-assign 和测试名单数据。
   - 缺少资源主档、考勤或实时在岗事实时明确降级展示。

7. **桌面和移动端适配**（本地与部分线上已验收）
   - 关键人工门、PMC 视图、活动流和制品预览支持桌面及移动浏览器。
   - 控制横向溢出、不可达内容、未处理错误和异常中断。

## 十、工资、工时和计件能力

1. **工资/工位/工时/单价 Agent 主链**（已实现）
   - 支持工资、工时、工位和单价查询。
   - 支持按姓名解析人员并查询工时。
   - 组长可在权限范围内登记本组成员的纯工时。
   - 无权操作时返回明确的权限提示，不暴露内部错误。

2. **单价版本历史与恢复**（已实现）
   - 支持单价版本查询、历史查看、恢复和跨轮确认。

3. **纯对话计件工资演示**（待实现）
   - 新对话中完成单价准备、确认和提交。
   - 完成多次工时与计件登记、工资单查询、锁定和报表导出。
   - 支持姓名/班组解析、统一工时与计件事件、幂等、锁定快照和导出回读。
   - 工人只能访问本人范围；厂长、财务和组长按角色授权。
   - 本期不承诺加班、阶梯计价、计时工资、税费和发薪。

## 十一、历史数据与辅助决策

1. **历史资料目录和身份治理**（待实现）
   - 建立来源目录、资源候选、历史产能画像和人工排产对照集。
   - 区分正式事实、候选、历史参考和测试数据身份。

2. **历史回放与校准**（待实现）
   - 使用历史订单、日产量和排产记录进行回放。
   - 输出交期、换型、资源利用率、WIP、计划稳定性等对照指标。
   - 历史数据只通过只读 Agent 工具按需使用，不得直接晋升 production 输入。

3. **测试数据包**（已完成测试设计）
   - W-H128 测试包覆盖订单、BOM、34 工位路线、99 条订单专属 WIP、人员、机器、模治具、日历、资格、速度、库存预约和预期结果。
   - 每个字段保留来源和身份，明确 `READY_FOR_TEST / PRODUCTION BLOCKED`。

## 十二、系统治理和交付支撑能力

这些内容属于系统运行与交付能力，不是用户业务流程，但仍在原文档的预期范围内。

1. 39084 应用、必要配置、数据库和持久化数据受控复刻到 39085。
2. 目标环境保留独立端口、身份、密钥、空 Redis 和默认关闭的外发/worker。
3. 部署前创建可恢复备份、检查点、数据库 dump、卷和镜像清单，并支持回滚验证。
4. 形成可迁移黄金发布包，包含代码版本、镜像、配置语义、数据包、Gate、说明书和回滚演练。
5. 生成订单上传到 PMC 的真实图文操作说明书和跨 Agent 维护交接资料。
6. 提供默认 dry-run 的通用云湃环境备份 Skill，支持发现、计划、备份、验证和恢复计划；默认不自动恢复。
7. 将 M7、M0、M1 按隔离端口、卷、GPU 和数据目录分流至 190 开发环境，并在 GB10 ARM 环境进行最终验证。
8. 将 GB10 39083 全套环境以独立 amd64 制品迁移至 190 39083，完成数据、身份、M7、GPU、FRP 单活和回滚验收。
9. 维护 GB10 swap、vLLM、MinerU、健康监控、自动回退和长期稳定性观察。

## 十三、当前明确不自动实现的能力

1. PMC 计算后的正式发布、派工和生产执行。
2. 未经人工确认的业务资料补全、规则变更、异常处置或候选应用。
3. 将模拟、fixture、pressure-only、历史参考或测试资源晋升为生产事实。
4. 生产订单取消后的自动预约释放、库存转公用、改绑或报废。
5. 首版逐工序原料投放、订单拆批、多线并行、跨线人员/设备/模治具调度。
6. 首版自动瓶颈修正、自动扩容、自动创建资源、跨线搬运、模治具寿命和技术人员排程。
7. M7 首版质检抽样、标准用量额度、复杂批次扣减和超领审批。
8. 首版真实邮件外发、ERP/SRM 写入、MES 接入或自动真实库存写入。
9. AI 自行修改硬约束、安全不变量、身份权限、审计规则或生产版本。

## 十四、仍需确认的关键口径

1. 换型能力是否进入首版，以及与“暂不加入换型系统”的历史决定如何统一。
2. 跨工位复用规则是否还有产品或工位类型方面的额外作用域限制。
3. Agent 可修改的“安全参数”准确范围。
4. M1–M4 各人工门的具体角色、权限和审计字段。
5. BOM 与 SOP 两个确认动作的前后顺序。
6. 公用在途采购在不同生产订单之间的分配规则。
7. 自动综合优先度的完整公式和同优先级排序规则。
8. 延期代价函数及特殊延期的业务决策边界。
9. M4 与真实 M7 到货、质检、入库接口的最终状态契约。

## 十五、原始功能 ID 覆盖索引

| 原始功能 ID | 本表归入位置 |
|---|---|
| F-INFRA-GB10-MEM-001 | 十二-9：GB10 模型和文档解析基础设施 |
| F-INFRA-190-DEV-001 | 十二-7：190 隔离开发环境 |
| F-INFRA-39083-MIGRATE-001 | 十二-8：39083 迁移 |
| F-39085-001 | 十二-1：应用与配置复刻 |
| F-39085-002 | 十二-1：数据库与持久化数据复刻 |
| F-39085-003 | 十：工资、工时、工位和单价 Agent |
| F-39085-004 | 十-2：单价版本历史与恢复 |
| F-39085-005 | 十二：全系统健康与入口验收 |
| F-39084-006 | 八：未知工艺人工允许后的 pressure-only PMC |
| F-39084-007 | 二-1：历史文档在线预览 |
| F-39084-008 | 十二-4：黄金发布包 |
| F-39084-009 | 十二-5：订单到 PMC 图文说明书 |
| F-39084-010 | 十二-5：前端维护与跨 Agent 交接 |
| F-39084-011 | 二-2：M0 源文件与 BOM/SOP 预览 |
| F-M5-PMC-DETAIL-001 | 九-6：Agent 生产来源详情卡片 |
| F-39085-006 | 六、八、九：受控兜底链同步 39085 |
| F-39085-007 | 八、九：通用资料缺口与 pressure-only PMC |
| F-39085-008 | 九-2：M1–M5 决策辅助卡片 |
| F-M7-CHAT-001 | 七-1：仓储对话状态机与临时库 |
| F-M7-CHAT-002 | 七-2 至七-4：送货识别和质检 |
| F-M7-CHAT-003 | 七-5：正常领料与超领 |
| F-39084-012 | 十-3：纯对话计件工资演示 |
| F-39084-013 | 二-3：工图与 SOP 工作台跳转 |
| F-39084-014 | 二-3：当前订单免上传直达预览 |
| F-REAL-ORDER-PMC-001 | 二至八：真实订单、BOM、SOP 到 production PMC |
| F-0825-M3-PO-REVIEW-001 | 六-1：采购单草稿人工复核 |
| F-0825-M4-MAIL-REVIEW-001 | 六-3：供应商邮件二次人工复核 |
| F-0825-DATA-SOURCE-001 | 五至八：数据来源和 production/pressure-only 双轨 |
| F-PMC-HISTORICAL-DATA-001 | 十一：历史资料利用与回放 |
| F-PMC-FINITE-SCHEDULING-001 | 八：有限产能 PMC |
| F-PMC-STREAMING-PILOT-001 | 八：W-H128 多订单流式 PMC 试点 |
| F-0825-LEGACY-READINESS-001 | 九-5、十二：TaskID、状态、制品、事件和开工门 |
| F-0825-ACTIVITY-STREAM-001 | 九-3：结构化活动流 |
| F-0825-FINAL-ACCEPTANCE-001 | 一至九：W-H128 端到端最终验收 |
| F-0826-LEGACY-FUNCTIONS-001 | 三至八、十四：老架构 M1–M5 业务规则 |
| F-39085-REBUILD-001 | 十二-2、十二-3：39085 冷备与重建 |
| F-39085-FULLCLONE-001 | 十二-1：39084 全量复刻至 39085 |
| F-BACKUP-SKILL-001 | 十二-6：通用环境备份 Skill |
| F-PMC-STREAMING-TEST-FIXTURE-001 | 十一-3：完整测试数据包 |
| F-PMC-AGENT-PARAMETER-GOVERNANCE-001 | 八-22：Agent 参数治理 |

## 十六、新增预期能力：Agent 受控自进化

当前原始文档没有完整自进化闭环。新增功能要求见 [Agent 受控自进化功能规格](AGENT_SELF_EVOLUTION_FUNCTION_SPEC.md)，核心包括：

1. 自动采集并脱敏工作过程、数据变化、工具调用、错误、纠正和结果事件。
2. 自动形成数据知识、操作经验、错误模式、反例、决策规则和评测用例候选。
3. 建立去重、聚类、冲突、失效传播、版本和混合索引。
4. 在后续相似任务开始、工具调用前和失败后主动检索并引用相关经验。
5. 在审批通过后修改白名单内的 `Agent.md`/`AGENTS.md`、声明式工作流、Prompt、参数、模板和检索策略。
6. 所有自修改经过最小 diff、基线哈希、静态验证、历史回放、影子测试、小流量灰度、正式晋升和可恢复回退。
7. 禁止自动修改源码、数据库迁移、依赖、身份权限、密钥、审计、安全不变量和外部写权限。

## 十七、新增预期能力：统一知识库与分级检索

统一知识库规格见 [Agent 统一知识库与分级检索功能规格](UNIFIED_KNOWLEDGE_BASE_FUNCTION_SPEC.md)，它是自进化系统的强制知识底座：

1. 统一保存或索引文件、数据库/API 记录、网页、工具事件、对话纠正、业务知识、操作经验、工作流版本和评测证据。
2. 原文件、原始记录或受控 source-only 引用是权威源；摘要只是导航，禁止摘要独存或替代原文。
3. 所有派生知识必须可追溯到页码、段落、表格/单元格、JSONPath、记录主键、日志事件或归档包条目。
4. 内容类型、可信等级、访问密级和作用域分别分类，不能混成一个等级。
5. 建立原文目录、全文、向量、结构化、关系图、错误签名和操作序列索引。
6. 检索按 Q0 权限过滤到 Q6 原文展开逐级进行，高风险判断、自修改和审批必须核对原文。
7. 支持纠正、冲突、替代、失效传播、知识发布快照、重新索引、停用、删除和回退。
8. 自进化系统不得另建不可审计的长期记忆，所有经验和提案必须引用统一知识库中的原文证据。
