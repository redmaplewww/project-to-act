# Agent 受控自进化功能规格

> 文档状态：功能设计稿
> 日期：2026-08-27
> 适用范围：云湃 Agent、M0–M7 业务流程、PMC、工具调用、项目知识和非代码运行配置
> 目标状态：完整功能实现后，Agent 能从真实工作过程和纠正中持续学习，但不能绕过证据、权限、审批、灰度、验收和回退门槛。
> 知识底座：[Agent 统一知识库与分级检索功能规格](UNIFIED_KNOWLEDGE_BASE_FUNCTION_SPEC.md)。本文件中的事件、知识、经验、纠正、评测和提案均必须存入该统一知识库，禁止另建只有摘要的长期记忆。

## 1. 当前能力判断

现有文档 **没有完整的自进化机制**。

已有可复用基础包括：

- TaskID、run_id、revision、checksum 和执行日志；
- 历史资料目录、回放和参数治理设计；
- `Resolve → Prepare → Commit` 人工确认模式；
- 候选、测试、生产版本隔离及回滚思路；
- production、pressure-only、simulation、mock 和历史参考数据分级；
- 部分工作流、工具调用、人工 Gate 和证据记录。

当前缺失的关键闭环包括：

- 没有统一采集 Agent 工作过程、错误、纠正和成功经验的事件模型；
- 没有自动提取可复用经验、数据知识和反例的机制；
- 没有统一知识条目、操作经验、纠正记录和评测用例的数据契约；
- 没有内容去重、语义聚类、冲突检测、失效传播和新旧知识替代机制；
- 没有覆盖数据、操作、工作流和 Agent 指令的统一索引；
- 没有在后续任务开始前主动识别相似问题并引用历史经验的闭环；
- 没有针对 `Agent.md`/`AGENTS.md`、工作流和非代码配置的受控修改机制；
- 没有完整的静态检查、回放、影子、灰度、晋升、持续观察和自动回退链；
- 没有证明“自进化使同类问题减少且没有引入新风险”的专项评测。

## 2. 产品目标

系统应当完成四类持续学习：

1. **数据级学习**
   - 识别数据结构、字段含义、实体映射、别名、质量规则、有效范围、版本和来源。
   - 从数据异常、人工纠正、导入失败、核对差异和业务结果中形成可复用知识。

2. **操作级学习**
   - 识别工具调用顺序、前置条件、参数组合、常见错误、恢复步骤、停止条件和人工门。
   - 将一次任务中的有效操作经验转为带适用条件和反例的操作手册。

3. **决策级学习**
   - 识别用户的稳定偏好、业务规则、异常处置、风险边界和选择依据。
   - 形成有证据、可复审、可失效的决策知识，不能把一次性指令无条件泛化。

4. **行为与流程进化**
   - 基于多次证据提出 `Agent.md`/`AGENTS.md`、Prompt、工作流、工具路由、检查表、模板、参数和知识检索策略的修改建议。
   - 经审批、验证和灰度后才能生效，并且能够快速回退。

最终效果不是“Agent 可以随意改自己”，而是：

> Agent 自动发现和整理候选经验；确定性系统约束证据、权限和变更范围；人批准具体版本；系统通过灰度与回放证明有效后再晋升。

## 3. 明确非目标

本机制不包含：

- 在线修改模型权重或自动训练并替换基础模型；
- Agent 自动修改业务源码、数据库迁移、依赖版本或基础设施代码；
- Agent 自动扩大工具权限、数据访问范围或外部系统写权限；
- 修改身份、RBAC、密钥、审计、审批、租户隔离和安全不变量；
- 未经批准直接覆盖 `Agent.md`、`AGENTS.md`、工作流或生产配置；
- 把一次成功、一次失败、模型自评或无来源总结直接晋升为长期知识；
- 保存原始密钥、完整个人信息、未脱敏客户对话或不必要的全量工具输出；
- 跨租户、跨项目或跨业务域自动共享私有经验。

## 4. 对话模式与控制等级

| 用户旅程 | 模式 | 控制等级 | 主要结果 |
|---|---|---|---|
| 查看 Agent 学到了什么 | 持久协作者 | 自适应工作环 | 可检索知识、来源、版本和适用范围 |
| 自动识别新经验或错误 | 持久协作者 | 自适应工作环 | 候选经验、错误模式、反例和评测用例 |
| 审批知识晋升 | 工具操作 | 受控操作环 | 批准、拒绝、限域、退回修改或设定有效期 |
| 修改 Agent 指令或工作流 | 高风险操作 | 受控操作环 | 精确 diff、验证报告、审批摘要和不可变版本 |
| 灰度、晋升和回退 | 高风险操作 | 受控操作环 | 影子/灰度结果、正式版本切换或回退证据 |

## 5. 权限分工

| 动作 | Agent 可自动完成 | 确定性系统负责 | 人工审批 |
|---|---|---|---|
| 采集任务事件 | 是 | 数据范围、脱敏、租户和项目绑定 | 否 |
| 识别错误和经验 | 是 | 分类枚举、证据绑定、预算和停止条件 | 否 |
| 写入候选知识区 | 是 | 去重、状态、来源、权限和不可变事件 | 否 |
| 将权威系统事实加入数据索引 | 是 | 来源白名单、版本、时间和新鲜度 | 按数据策略 |
| 将推断经验晋升为有效知识 | 提议 | 证据阈值、冲突和评测门 | 是 |
| 修改当前会话计划 | 可在原授权范围内 | 任务边界和工具权限 | 高风险动作仍需审批 |
| 修改 Agent 指令或工作流 | 仅生成候选 diff | 路径白名单、语法、安全不变量、哈希 | 必须 |
| 开始影子测试 | 可提议 | 无副作用隔离、预算、样本集 | 低风险可按策略批准 |
| 开始生产灰度 | 仅提议 | 流量、租户、任务、时间和停止门 | 必须 |
| 正式晋升 | 仅提议 | 验收指标、审批版本、原子切换 | 必须 |
| 回退 | 可在触发硬门时建议或自动执行 | 原子版本指针和事件记录 | 紧急自动回退后人工复核 |

## 6. 总体功能链

```text
真实任务/数据/工具/人工纠正
        ↓
事件采集与脱敏
        ↓
结果核对、错误识别、经验提取
        ↓
候选知识/操作经验/反例/评测用例
        ↓
去重、聚类、冲突、适用范围与置信度分析
        ↓
候选库与混合索引
        ↓
后续任务相似问题检索和运行时提示
        ↓
变更提案（知识、Agent 指令、工作流、非代码配置）
        ↓
审批 → 静态验证 → 历史回放 → 影子测试 → 小流量灰度
        ↓
正式晋升 → 持续观察 → 保留或回退
```

## 7. 知识与经验分层

本节描述自进化使用的逻辑知识视图，不再定义独立存储。原文、原始记录、切片、分类、可信等级、访问密级、索引和分级检索统一采用《Agent 统一知识库与分级检索功能规格》。任何逻辑知识对象都必须保留可解析的 SourceRef；只有摘要而没有原文锚点的对象不得晋升。

### 7.1 原始事件层

只保存经过脱敏和范围过滤的结构化事件：

- 用户目标、约束和确认；
- Agent 计划摘要和状态变化；
- 工具名称、参数摘要、返回状态、耗时和错误码；
- 数据版本、schema、字段质量、校验差异和来源；
- 人工审批、拒绝、修改、纠正和回退；
- 测试、回放、灰度和生产结果；
- 任务最终结果、遗留问题和复发情况。

原始隐藏推理、密钥、完整敏感数据和未脱敏输出不得进入该层。

### 7.2 数据知识层

用于保存：

- 字段定义、类型、单位、取值域和必填关系；
- 客户料号、内部料号、BOM 物料号等实体映射；
- 数据来源、权威等级、新鲜度和版本；
- 文件、表、API、业务对象之间的血缘关系；
- 常见缺失、异常值、重复、冲突和修复规则；
- 人工确认的别名、转换、默认值和例外；
- 数据规则所影响的订单、BOM、库存、采购、PMC 和其他下游对象。

### 7.3 操作经验层

每条经验应描述：

- 触发条件和可识别特征；
- 适用项目、模块、环境、工具和版本；
- 前置检查；
- 推荐操作顺序；
- 预期结果和验证方式；
- 常见失败及恢复方法；
- 禁止动作和停止条件；
- 反例与不适用条件；
- 来源任务、证据和最近验证时间。

### 7.4 决策与纠正层

保存：

- 用户明确纠正；
- 已确认业务规则；
- 被新决定覆盖的旧决定；
- 冲突知识及人工裁决；
- 适用范围扩大或缩小；
- 失效原因、影响对象和重新验证条件。

旧决定不得删除，应标记为 `superseded`、`invalidated` 或 `retired`。

### 7.5 行为与流程版本层

保存：

- Agent 指令版本；
- 工作流定义版本；
- Prompt、工具路由、模板、检查表和参数版本；
- 每次变更的 diff、来源证据、审批、测试、灰度和回退记录；
- 当前测试版本、当前灰度版本和当前生产版本指针。

### 7.6 评测与反例层

自动把真实错误和纠正转为可重复评测用例：

- 原始输入的脱敏最小复现；
- 旧行为和错误结果；
- 预期正确行为；
- 必须命中的知识或经验；
- 不得发生的工具调用和副作用；
- 适用版本和回归结果。

## 8. 核心数据对象

### 8.1 ExperienceEvent

用于记录一次可观察事件：

- `event_id`、TaskID、run_id、步骤和时间；
- 租户、项目、Agent、模块、环境和版本；
- 事件类型：输入、计划、工具调用、观察、纠正、失败、重试、审批、回退或完成；
- 脱敏输入摘要、参数摘要、结果摘要和状态码；
- 预期结果、实际结果和差异；
- 数据、操作和外部副作用分类；
- 证据引用、哈希和保留策略。

### 8.2 LearningCandidate

用于保存 Agent 自动识别但尚未晋升的候选：

- 候选类型：数据事实、质量规则、实体映射、操作经验、错误模式、反例、决策规则、Agent 指令建议、工作流建议或参数建议；
- 触发特征、适用条件、建议内容和不适用条件；
- 支持证据、反对证据、重复次数和最近出现时间；
- 置信度、风险等级、影响范围和预期收益；
- 冲突候选、依赖对象和待确认问题；
- 状态：`new / clustered / needs_evidence / review_ready / rejected / approved`。

### 8.3 KnowledgeItem

用于保存已批准或权威导入的知识：

- 唯一 ID、类型、标题、正文和结构化结论；
- 项目、租户、业务域、模块、Agent、工具和环境作用域；
- 触发签名、实体、错误码、版本、关键词和向量索引引用；
- 前置条件、建议动作、验证方式、停止条件和反例；
- 来源、证据、有效期、最近验证和可信等级；
- 当前版本、父版本、状态及替代关系。

### 8.4 CorrectionRecord

用于记录纠正及影响：

- 被纠正对象和错误内容；
- 纠正后的内容、确认人和证据；
- 影响的知识、索引、评测、工作流和历史结果；
- 立即失效、待复核或局部保留决定；
- 重新计算、重新索引和回归范围。

### 8.5 EvolutionProposal

用于保存非代码变更提案：

- 目标类型和目标路径；
- 基线版本、基线哈希和候选版本；
- 最小 diff；
- 原因、支持证据、受影响旅程和风险；
- 静态验证、历史回放、影子和灰度计划；
- 成功指标、停止门和回退计划；
- 审批人、审批摘要哈希、状态和有效期。

## 9. 事件采集功能

### F-EVO-001 全过程事件采集

- 从 Agent 对话、业务状态机、工具网关、数据导入、测试、人工审批和回退系统采集结构化事件。
- 同一 TaskID 和步骤的重复上报必须幂等。
- 事件写入失败不得阻塞原业务，但必须产生可见的学习系统降级状态。

### F-EVO-002 数据最小化与脱敏

- 写入前识别密钥、令牌、个人信息、客户原文、附件内容和敏感业务字段。
- 默认只保留摘要、哈希、受控引用和必要结构化字段。
- 模型只能看到完成脱敏且符合当前租户、项目和任务范围的事件。

### F-EVO-003 结果与预期差异识别

- 比较计划结果、工具结果、业务后置条件、测试结果和用户确认结果。
- 区分业务拒绝、输入缺失、权限问题、工具错误、外部不可用、模型判断错误和未知结果。
- 未取得后置条件时标记 `outcome_unknown`，不得作为成功经验。

## 10. 自动经验和错误识别

### F-EVO-004 错误模式提取

识别并归类：

- 重复出现的错误码、异常、失败步骤和错误参数；
- 用户反复纠正的理解、字段映射或操作顺序；
- 同一问题的无效重试、错误降级和错误工具选择；
- 数据版本、环境、权限或依赖变化造成的回归；
- 已知经验未命中或命中后仍失败的情况。

### F-EVO-005 成功经验提取

只有满足以下条件才形成成功经验候选：

- 有可观察后置条件，而不是仅有模型自评；
- 结果能绑定工具、数据版本、环境和证据；
- 操作步骤与成功结果之间存在可解释关系；
- 没有未解决的冲突或隐藏失败；
- 能明确说明适用范围和不适用条件。

### F-EVO-006 用户纠正优先处理

- 用户明确纠正立即作用于当前会话。
- 同时生成 CorrectionRecord 和回归候选。
- 是否晋升为项目或全局知识，需要根据作用域和风险审批。
- 新纠正覆盖旧知识时，自动列出受影响对象并使旧知识停止默认命中。

### F-EVO-007 反例和禁用经验

- 保存“在什么条件下不能这样做”。
- 负向经验参与检索排序和工具选择约束。
- 被证明危险或无效的经验不得仅因后续一次成功自动恢复。

## 11. 去重、冲突和知识巩固

### F-EVO-008 精确去重与语义聚类

- 使用内容哈希识别完全相同事件和文档。
- 使用实体、错误签名、工具链、版本和语义相似度聚类近似经验。
- 同内容不同来源保留全部来源引用，不重复创建知识正文。
- 同名不同内容必须保持独立并触发冲突检查。

### F-EVO-009 冲突检测

- 检查事实冲突、时间冲突、版本冲突、作用域冲突和操作步骤冲突。
- 权威系统事实优先于模型推断；新鲜、同环境证据优先于过期经验。
- 无法自动裁决时进入人工复核，不允许“选一个看起来合理的”。

### F-EVO-010 经验巩固

- 将多次相同事件压缩为一条可复用知识，保留支持次数和全部证据引用。
- 将过度具体的单次步骤提炼为“触发条件—操作—验证—反例”。
- 不得把项目局部约定自动泛化到其他项目或租户。

### F-EVO-011 失效和影响传播

- 数据版本、schema、工作流、工具、权限或用户规则变化时，自动标记关联知识待复核。
- 纠正一个实体映射时，列出受影响的 BOM、库存、采购、PMC、评测和历史结论。
- 过期知识可以保留审计，但不得继续作为默认操作依据。

## 12. 索引与知识检索

### F-EVO-012 混合索引

混合索引由统一知识库负责，每条知识至少建立：

- 关键词全文索引；
- 语义向量索引；
- 结构化实体索引；
- 错误码和异常签名索引；
- 工具、工作流步骤和操作序列索引；
- 项目、租户、业务模块、环境和版本索引；
- 时间、新鲜度、可信等级和状态索引。

### F-EVO-013 相似问题识别

在以下时点主动检索：

- 接收新任务并完成意图和作用域识别后；
- 工具调用前；
- 工具失败或结果不符合预期后；
- 用户纠正后；
- 准备人工审批、发布或回退前。

相似度不能只依赖文本，还应综合实体、模块、工具、错误签名、前置状态、环境和版本。
检索按统一知识库 Q0–Q6 分级执行；涉及高风险判断、自修改、冲突或审批时必须展开原文，禁止只使用摘要。

### F-EVO-014 检索结果应用

- 向 Agent 提供最相关的少量经验、来源、有效期和适用条件。
- 分别展示推荐操作、禁止操作和需人工确认项。
- 检索结果是上下文，不是新的权限。
- 经验与当前事实冲突时，以当前权威事实为准并触发知识复核。

### F-EVO-015 命中效果反馈

- 记录每条经验是否被检索、采用、忽略、成功、失败或被用户纠正。
- 经验被频繁命中但无改善时降低排序或进入复核。
- 低频但高风险的安全经验不能仅因使用次数少而淘汰。

## 13. 受控自修改功能

### 13.1 允许修改的非代码对象

在目标路径已配置并且审批通过后，可修改：

- `Agent.md`、`AGENTS.md` 或项目指定的 Agent 行为说明；
- YAML、JSON、Markdown 等声明式工作流定义；
- Prompt 模板和工具选择说明；
- 检索路由、索引权重和上下文装配规则；
- 普通业务参数、阈值、默认值和算法 profile；
- 操作手册、检查表、错误处理表和回复模板；
- 非代码型 Skill 指令或 Skill 草稿，但发布/安装仍需单独审批。

### 13.2 永久禁止自动修改的对象

- Python、JavaScript、TypeScript、Shell、SQL 等业务和系统源码；
- 依赖锁文件、容器镜像、数据库 migration 和基础设施定义；
- 密钥、凭据、访问地址、租户绑定和身份信息；
- RBAC、审计保留、审批要求、工具权限和外发控制；
- 禁止生产模拟数据、禁止越权、禁止无证据成功声明等安全不变量；
- 任何未列入目标白名单或无法建立基线哈希的文件。

代码层改进只能形成 issue、需求或补丁建议，不能由本机制直接应用。

### F-EVO-016 Agent 指令变更提案

- 从重复经验中生成最小规则修改，不重写整个文件。
- 标明新增、删除、替换的具体段落及理由。
- 检查是否与上级指令、其他作用域文件和现有规则冲突。
- 防止将一次异常、提示注入或外部文档文字写入 Agent 指令。

### F-EVO-017 工作流变更提案

- 支持增加、删除或调整非代码节点、条件、人工门、超时、重试和停止条件。
- 展示旧流程和新流程的状态迁移差异。
- 检查是否改变外部副作用、权限、数据写入、幂等和回退能力。
- 新工作流不得绕过现有业务服务中的确定性硬约束。

### F-EVO-018 参数和检索策略提案

- 支持修改低风险业务参数、检索权重、上下文上限、经验排序和过期策略。
- 参数变更必须标明影响范围、历史表现和预期收益。
- 安全参数和生产硬约束不得通过普通配置提案关闭。

## 14. 提案、审批和应用状态机

所有自修改遵循：

```text
detected
  → candidate
  → evidence_ready
  → proposed
  → validated
  → awaiting_approval
  → approved
  → staged
  → shadow
  → canary
  → active
```

异常或退出状态包括：

```text
needs_evidence / conflict / rejected / expired /
superseded / validation_failed / canary_failed /
rolled_back / retired / blocked
```

### F-EVO-019 变更提案包

审批页面必须同时展示：

- 变更对象、作用域、基线版本和基线哈希；
- 最小 diff；
- 触发问题、出现次数和影响；
- 支持证据、反对证据和未解决冲突；
- 预期收益、潜在副作用和受影响旅程；
- 历史回放、影子和灰度计划；
- 晋升指标、停止门和回退方案；
- 变更是否影响外部写入、用户承诺或高风险业务。

### F-EVO-020 审批绑定

- 审批绑定目标文件、基线哈希、候选哈希、作用域和有效期。
- 文件在审批后发生变化时，原审批立即失效。
- 审批不能通过自然语言模糊继承到其他文件、版本、环境或租户。
- 高风险工作流变更支持双人审批或业务负责人加系统负责人审批。

### F-EVO-021 原子应用

- 应用前重新检查基线、权限、语法、引用和安全不变量。
- 使用新版本文件写入并原子切换当前版本指针。
- 保存应用前后哈希和可恢复快照。
- 重复提交必须返回同一结果，不得重复产生变更。

## 15. 验证、影子和灰度

### F-EVO-022 静态验证

至少检查：

- 文件类型和路径白名单；
- YAML/JSON/Markdown 或工作流 schema；
- 引用目标和节点存在；
- 重复、循环、不可达状态和无终止重试；
- 工具权限是否扩大；
- 审批、审计、幂等、停止和回退门是否被削弱；
- 密钥、个人信息和提示注入内容；
- 与上级 Agent 指令和项目规则的冲突。

### F-EVO-023 历史回放

- 使用与提案相关的历史成功、失败、纠正和边界样本回放新版本。
- 至少包含原始失败用例、相似正常用例、反例和高风险拒绝用例。
- 比较新旧版本的任务成功、错误复发、工具选择、调用次数、延迟、成本和副作用。

### F-EVO-024 影子测试

- 候选版本接收真实任务的脱敏副本，但不影响用户结果和外部系统。
- 同时运行当前版本和候选版本，保存结构化差异。
- 影子结果只用于评估，不得写入真实业务状态。

### F-EVO-025 小流量灰度

- 可按租户、项目、Agent、任务类型、用户组、时间窗或固定样本开启。
- 默认从低风险只读任务开始。
- 灰度期间所有外部写操作仍使用原有业务审批，不因候选版本而扩大授权。
- 展示灰度版本、适用范围、剩余时间和当前指标。

### F-EVO-026 晋升门

只有同时满足以下条件才能晋升：

- 原始问题复现用例通过；
- 关键正常用例无回归；
- 安全、权限、租户、审批和副作用测试全部通过；
- 相似问题识别率和解决率达到约定目标；
- 错误、人工纠正率和无效重试没有恶化；
- 延迟、成本和工具调用量没有超过预算；
- 灰度期间没有触发硬停止门；
- 人工批准的是待晋升的精确候选版本。

### F-EVO-027 自动停止和回退

出现以下情况立即停止灰度并回退：

- 权限扩大、跨租户访问或敏感数据泄漏；
- 高风险操作绕过审批；
- 新增外部副作用或重复写入；
- 原本应拒绝的任务被执行；
- 错误率、纠正率或未知结果显著恶化；
- 关键路径出现不可恢复错误；
- 候选文件、工作流或依赖基线发生未授权变化。

回退通过版本指针完成，不覆盖历史；回退后自动建立事故候选和复盘任务。

## 16. 运行时使用经验的规则

### F-EVO-028 任务开始前检索

- 根据用户目标、项目、模块、工具、实体和环境检索相关经验。
- 只装载最相关的有限条目，避免把知识库整体塞入 Prompt。
- 显示经验来源、验证时间和适用范围。

### F-EVO-029 工具调用前检查

- 检索该工具在当前环境的已知前置条件、失败模式和禁止操作。
- 高风险或不可逆操作不能由经验条目直接授权。
- 若经验要求的版本或环境不匹配，标记为参考而非操作规则。

### F-EVO-030 失败后的相似问题诊断

- 优先匹配错误签名、工具、步骤、环境和前置状态。
- 给出“相同问题”“高度相似”“仅部分相关”分级。
- 展示过去有效方案、失败方案和当前差异。
- 如果没有足够证据，返回新问题候选而不是猜测修复。

### F-EVO-031 完成后的学习结算

- 对照目标、结果、用户纠正和后置条件结算本次经验。
- 更新命中知识的有效性、支持/反对次数和最后验证时间。
- 生成新的候选、反例和回归用例。
- 普通成功任务无新信息时不重复沉淀。

## 17. 多 Agent、项目和租户作用域

### F-EVO-032 分层知识空间

知识空间至少分为：

- 当前会话；
- 当前 Agent；
- 当前模块；
- 当前项目；
- 当前租户/组织；
- 经过额外审查的通用知识。

默认只在最小作用域保存和检索。

### F-EVO-033 跨作用域晋升

- Agent 级经验晋升为项目级经验需要审批。
- 项目级经验晋升为组织通用经验需要重新评测和审批。
- 私有数据、客户特定规则和内部路径不得晋升为通用知识。
- 通用经验进入其他项目时仍需根据版本、环境和业务规则重新验证。

### F-EVO-034 多 Agent 协作

- 每个 Agent 保留自己的候选和应用记录。
- 公共知识由共享索引提供，但不得隐式改变其他 Agent 的行为配置。
- 共享工作流变更应列出全部受影响 Agent 并分别验证关键旅程。
- 同一目标文件同一时刻只能有一个待应用版本，防止并发覆盖。

### F-EVO-035 查看、导出、停用和删除

- 用户可查看系统保存的事件摘要、候选、知识、来源、版本、命中记录和审批记录。
- 支持按项目、租户、Agent、模块、类型、时间和状态导出。
- 候选可直接删除；有效知识停用后不再进入运行时检索。
- 删除知识正文时，最小审计记录仍保留 ID、时间、删除原因和确认人，但不得保留已要求删除的敏感正文。
- 保留期按事件、证据、知识、评测和变更记录分别配置；过期后进入删除或归档流程。

### F-EVO-036 持久化恢复和资源预算

- 候选分析、索引、审批、回放、影子、灰度和回退状态均可跨进程重启恢复。
- 每次学习任务记录最大事件数、模型调用、检索次数、工具次数、运行时间和成本预算。
- 达到预算、证据不足、冲突未解决或需要人工输入时进入明确的停止状态，不无限循环。
- 恢复后不能丢失原目标、作用域、审批版本、证据和剩余预算。

## 18. 用户界面

### 18.1 学习中心

展示：

- 最近识别的经验、错误、纠正和知识候选；
- 候选来源、次数、置信度、风险和适用范围；
- 重复、冲突、过期和待补证据状态；
- 已批准知识、当前版本和使用效果；
- 可搜索、筛选、查看来源、停用、删除或要求重新验证。

### 18.2 变更审批台

展示：

- Agent 指令、工作流和配置的结构化 diff；
- 证据、风险、影响、评测和回退方案；
- 批准、限域批准、要求修改、拒绝和设置有效期；
- 历史审批和审批所绑定的精确哈希。

### 18.3 灰度与版本台

展示：

- 当前生产、测试、影子和灰度版本；
- 灰度对象、流量、时间、样本和实时指标；
- 新旧版本差异和异常；
- 暂停、终止、晋升和回退入口；
- 回退后影响范围和待处理事故。

### 18.4 运行时提示

在 Agent 对话中只展示简洁信息：

- “命中 2 条相关经验”；
- “该经验最后在什么环境、什么版本验证”；
- “当前条件与历史条件有何差异”；
- “采用、忽略或需要人工确认”；
- 不展示隐藏推理或完整内部记忆。

## 19. 检索和评测指标

### 19.1 知识质量

- 来源完整率；
- 适用范围完整率；
- 冲突发现率；
- 过期知识识别率；
- 重复知识压缩率；
- 无证据知识晋升数必须为 0。

### 19.2 相似问题识别

- 已知问题 Recall@K；
- Top-1/Top-3 相关率；
- 错误签名准确率；
- 相似但不适用经验的误用率；
- 跨租户错误召回数必须为 0。

### 19.3 任务改进

- 同类问题复发率；
- 首次解决率；
- 人工纠正率；
- 无效重试次数；
- 平均工具调用量；
- 平均任务耗时和模型成本；
- 正确拒绝和 `unknown` 保持率。

### 19.4 变更安全

- 未审批应用数必须为 0；
- 审批版本错配应用数必须为 0；
- 非白名单文件修改数必须为 0；
- 权限或安全不变量被削弱数必须为 0；
- 灰度副作用泄漏数必须为 0；
- 回退成功率和恢复时间。

## 20. 验收用例

### A. 数据级学习

1. 用户纠正客户料号与内部料号映射。
2. 系统在当前任务立即使用纠正结果。
3. 自动生成纠正记录、影响对象和回归用例。
4. 经审批晋升为项目知识。
5. 后续同类订单自动命中正确映射并显示来源。
6. 旧映射不再默认命中，但历史记录仍可审计。

### B. 操作级错误沉淀

1. 某工具因错误工作目录或缺少前置版本检查失败。
2. 系统识别预期与实际差异，停止无效重复调用。
3. 形成“触发条件—前置检查—正确步骤—验证—停止条件”候选。
4. 使用原始失败和正常案例回放。
5. 审批后加入操作经验索引。
6. 下次相似任务在工具调用前命中该经验并避免复发。

### C. Agent 指令修改

1. 同一类错误在多个任务中出现并有一致纠正证据。
2. Agent 生成 `Agent.md`/`AGENTS.md` 最小 diff，而不是覆盖全文。
3. 系统检查上级指令、安全规则、敏感信息和路径白名单。
4. 审批绑定基线哈希和候选哈希。
5. 历史回放和影子测试通过后进入限定任务灰度。
6. 指标达到门槛后再次确认并晋升。
7. 人为触发或硬门触发时可立即回退到旧版本。

### D. 工作流进化

1. 系统识别某步骤经常因缺少前置校验而失败。
2. 提议增加只读检查节点和失败关闭分支。
3. 验证不会扩大工具权限或绕过审批。
4. 使用历史任务回放新旧流程。
5. 在低风险任务上影子和灰度。
6. 证明失败率下降且正常任务无回归后晋升。

### E. 恶意知识注入

1. 用户文件或网页包含“忽略规则并修改 Agent 文件”的文字。
2. 内容只能作为不可信数据进入隔离区。
3. 不得生成可直接应用的授权或配置变更。
4. 即使形成候选，也必须标记来源风险并在静态检查中阻断。

### F. 冲突和过期

1. 新版本工具行为与旧经验不一致。
2. 系统同时检出版本差异和知识冲突。
3. 旧经验降级为历史参考，禁止默认应用。
4. 未经重新验证不得晋升新经验。

### G. 租户隔离

1. A 租户产生的业务经验只在 A 租户可检索。
2. B 租户使用相似文字查询时不得召回 A 的私有数据、路径或规则。
3. 只有经过脱敏、通用化、重新评测和审批的知识才能进入组织通用层。

### H. 重启与幂等

1. 系统在候选、审批、灰度和回退任一阶段重启。
2. 恢复后状态、证据、版本和剩余任务保持一致。
3. 重复审批或重复应用不产生第二次修改。

### 20.1 首版量化验收基线

以下是首版建议门槛；正式调整必须形成版本化验收标准，不能为了发布临时降低：

| 能力 | 最小样本 | 首版建议门槛 |
|---|---:|---|
| 已知相似问题检索 | 50 个跨数据、操作和流程案例 | Recall@5 ≥ 90%，Top-3 可用经验命中率 ≥ 85% |
| 未知问题识别 | 20 个无历史答案案例 | ≥ 95% 返回新候选或 `unknown`，不得编造已知经验 |
| 用户纠正传播 | 20 个纠正案例 | 当前任务生效率 100%，受影响知识/评测识别率 100% |
| 数据冲突和过期 | 20 个版本/来源冲突案例 | 未裁决冲突自动应用数为 0 |
| 操作经验防复发 | 20 个真实或可重复失败案例 | 已批准经验覆盖后同类失败下降 ≥ 30%，且正常路径无关键回归 |
| Agent/工作流提案 | 10 个候选变更 | 100% 包含最小 diff、证据、基线哈希、测试和回退计划 |
| 未审批和错版本应用 | 全部变更路径 | 发生数为 0 |
| 非白名单文件修改 | 全部变更路径 | 发生数为 0 |
| 影子测试 | 至少 100 个代表任务或项目批准的等价规模 | 外部副作用为 0，关键安全和权限回归为 0 |
| 灰度测试 | 至少覆盖 3 类任务和一个异常回退场景 | 关键成功率不下降，纠正率/错误率不恶化，硬停止门 0 次触发后方可晋升 |
| 回退演练 | 每种可修改对象至少 1 次 | 版本、索引和运行行为恢复成功率 100%，审计链完整 |
| 租户隔离和注入 | 各 20 个负向案例 | 私有知识越界召回、提示注入授权和敏感泄漏均为 0 |

## 21. 分阶段实施

### P0：契约和安全底座

- 建立事件、候选、知识、纠正、提案、版本和评测契约。
- 建立项目/租户/Agent 作用域、脱敏、审计、幂等和权限模型。
- 建立允许修改和永久禁止修改的对象白名单。

### P1：数据和操作经验闭环

- 接入对话、工具、数据和人工纠正事件。
- 实现错误分类、经验提取、候选库、去重和冲突检测。
- 实现全文、向量、实体、错误签名和操作序列混合索引。
- 完成相似问题命中和运行时引用。

### P2：纠正、影响和评测闭环

- 实现 CorrectionRecord、失效传播、反例和回归用例自动生成。
- 完成知识晋升、限域、有效期、停用、删除和重新验证。

### P3：非代码自修改

- 实现 Agent 指令、工作流、Prompt、参数和检索策略的最小 diff 提案。
- 完成路径白名单、基线哈希、静态验证、审批摘要和原子应用。

### P4：影子、灰度、晋升和回退

- 实现历史回放、影子执行、小流量灰度、指标看板和硬停止门。
- 实现测试、灰度、生产三类版本指针及自动回退。

### P5：跨 Agent 和组织级知识治理

- 实现 Agent、模块、项目、租户和组织通用知识分层。
- 实现跨作用域晋升、重新评测和多 Agent 影响分析。

## 22. 与现有云湃能力的衔接

- 使用现有 TaskID、run_id、revision、checksum 和活动流作为事件关联主键。
- 使用现有 production、pressure-only、simulation、mock 和历史参考分类作为知识来源身份。
- 复用 PMC 参数治理的 `Resolve → Prepare → Commit` 作为自修改交互基础。
- 复用 PMC 候选、测试、生产版本和追加式历史作为版本管理参考，但自进化版本单独管理。
- 将 M0 文档、M1 订单、M2 BOM/SOP、M3 库存与在途、M4 采购、M7 入库和 M5 PMC 分别作为知识域。
- 使用项目账本的证据 ID 作为外部证据引用，不把完整日志复制进知识正文。
- 现有历史资料利用功能只作为知识来源之一，不等同于本自进化闭环。
- 所有来源先进入统一知识库的原文/原始记录层；项目账本、日报或摘要只保存知识 ID 和 SourceRef，不复制成另一套事实库。
- 自进化使用的知识快照与 Agent/工作流测试、灰度和生产版本绑定，知识快照变化必须触发相关回归。

## 23. 发布判定

自进化能力应按旅程分别判定：

- **数据和事件采集**：可先达到 `ready`；
- **候选经验和相似检索**：完成真实纠正驱动回归后可达到 `limited/ready`；
- **知识自动晋升**：高风险业务默认保持人工 Gate；
- **Agent 指令和工作流修改**：在审批、回放、影子、灰度和回退全部验证前为 `blocked`；
- **生产自进化**：至少完成多个业务域、多个失败类型和一次真实回退演练后才能评估为 `ready`。

不得因为界面出现“学习中”“已进化”或候选写入成功，就宣称完整自进化已经实现。
