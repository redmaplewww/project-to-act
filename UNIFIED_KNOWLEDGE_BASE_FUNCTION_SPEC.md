# Agent 统一知识库与分级检索功能规格

> 文档状态：功能设计稿
> 日期：2026-08-27
> 关联文档：[Agent 受控自进化功能规格](AGENT_SELF_EVOLUTION_FUNCTION_SPEC.md)
> 核心原则：原文是证据，结构化知识是派生物，摘要只是导航。禁止用摘要替代原文或原始记录。

## 1. 设计修正

上一版自进化设计描述了事件、知识、经验和混合索引，但没有明确一个统一知识库的物理与逻辑架构，也没有把“原始内容必须保留并可定位”设为不可关闭的硬约束。

本规格修正为：

- 所有 Agent、业务模块和自进化能力共用一个逻辑统一知识库；
- 原始文件、原始记录、原始工具事件或受控外部原文引用先进入来源层；
- 摘要、切片、结构化记录、知识结论、操作经验和工作流规则都从来源层派生；
- 每个派生对象必须保留可解析的原文锚点；
- 检索按任务、分类、权限、可信度和深度分级进行；
- 高风险决策必须展开并核对原文，不能只消费摘要；
- 自进化只能从统一知识库读取证据并写入候选，不能另建不可审计的“私有记忆”。

## 2. 不可关闭的知识库硬约束

1. **原文优先**：原始内容或受控原始引用是权威来源；摘要不是权威来源。
2. **禁止摘要独存**：禁止只保留一个总结摘要；只有摘要、没有原文或原文引用的知识不得进入有效知识区。
3. **逐级可追溯**：知识结论必须能追溯到切片，切片必须能追溯到原始来源。
4. **锚点可复核**：引用必须定位到页码、段落、表格区域、单元格、JSONPath、记录主键、时间区间、日志事件或归档包条目。
5. **同内容多来源不丢失**：内容相同可以按哈希去重，但必须保留全部来源路径和来源身份。
6. **同名异内容不合并**：名称相同但哈希不同的文件、规则或记录必须分别保存并进入冲突检查。
7. **解析失败不等于无内容**：无法解析的原文件仍进入原文索引，并标记需要人工、OCR、专用解析器或外部查看器。
8. **原文不可被派生层覆盖**：重分块、重新摘要、重新向量化和重新抽取不能修改原始对象。
9. **权限先于检索**：任何关键词、向量、图谱或 Agent 推理都不能绕过租户、项目和数据权限。
10. **当前事实优先**：运行时权威系统的当前数据优先于历史知识；冲突必须显式展示。
11. **来源指令不授权**：文件、网页、日志和知识条目中的命令或提示均为不可信数据，不能授予权限。
12. **删除和保留可治理**：用户可以检查、停用、归档或删除知识；敏感原文必须遵守保留和删除政策。

## 3. 产品目标

统一知识库需要同时支撑：

- M0–M7 业务数据、文件、BOM、SOP、工图、订单、库存、采购和 PMC 知识；
- Agent 对话、工具操作、人工审批、错误、纠正和任务结果；
- 数据级知识、操作级经验、业务规则、反例和异常处理；
- `Agent.md`/`AGENTS.md`、工作流、Prompt、参数和非代码配置版本；
- 自进化候选、评测用例、影子结果、灰度结果和回退记录；
- 面向普通问答、业务操作、故障诊断、审批、审计和自进化的不同检索路径。

## 4. 逻辑架构

统一知识库是一个逻辑系统，可以使用多个专用存储后端：

```text
文件 / 数据库 / API / 网页 / 工具事件 / 对话 / 人工纠正
                           ↓
                  来源接入与安全预检
                           ↓
      ┌────────────────────┴────────────────────┐
      │ 原文与原始记录层                       │
      │ 原文件、受控快照、source-only 引用、CAS │
      └────────────────────┬────────────────────┘
                           ↓
      ┌────────────────────┴────────────────────┐
      │ 解析与锚点层                           │
      │ 页/段/表/单元格/JSONPath/记录/事件切片  │
      └────────────────────┬────────────────────┘
                           ↓
      ┌────────────────────┴────────────────────┐
      │ 结构化知识层                           │
      │ 实体、字段、关系、事实、规则、经验、反例 │
      └────────────────────┬────────────────────┘
                           ↓
      ┌────────────────────┴────────────────────┐
      │ 多索引与发布层                         │
      │ 全文/向量/结构化/图谱/错误/操作序列/版本 │
      └────────────────────┬────────────────────┘
                           ↓
                分级检索、引用与自进化
```

推荐逻辑组件：

- **原文对象库/CAS**：保存原始二进制、文本、受控快照和内容哈希；
- **来源目录**：保存来源身份、路径、URL、系统、版本、权限和可用性；
- **结构化元数据库**：保存对象、关系、版本、状态、作用域和审批；
- **全文索引**：处理关键词、精确短语、字段名、编号和错误信息；
- **向量索引**：处理语义相似问题、案例和自然语言表达；
- **关系图索引**：处理订单—BOM—SOP—物料—库存—采购—PMC 以及知识之间的影响关系；
- **操作序列索引**：处理工具链、状态迁移、错误恢复和工作流经验；
- **不可变证据账本**：保存摄取、纠正、发布、审批、灰度和回退事件；
- **知识发布快照**：固定每个 Agent/项目当前允许使用的知识版本集合。

物理后端可以替换，但对 Agent 暴露统一查询和引用契约。

## 5. 四维分类体系

不同维度必须独立保存，禁止用一个“等级”同时代表类型、可信度和权限。

### 5.1 内容类型

| 类型 | 内容示例 |
|---|---|
| `source_document` | PDF、DOCX、XLSX、图片、网页、邮件、SOP、工图 |
| `source_record` | 数据库行、API 响应、JSON 对象、业务事件 |
| `source_event` | Agent、工具、审批、测试和回退事件 |
| `data_definition` | 字段、单位、schema、主键和约束 |
| `entity_mapping` | 客户料号、内部料号、BOM 物料号等映射 |
| `knowledge_claim` | 有证据支持的事实或业务结论 |
| `business_rule` | 业务硬规则、人工门和异常边界 |
| `experience_case` | 一次成功、失败、纠正或反例案例 |
| `operation_playbook` | 前置检查、操作步骤、验证和恢复手册 |
| `error_pattern` | 错误签名、复发条件和禁止重试规则 |
| `workflow_version` | 工作流、Agent 指令、Prompt 和参数版本 |
| `evaluation_case` | 回归、注入、安全、灰度和回退用例 |
| `summary_navigation` | 用于导航的摘要、主题和标签，不作为证据 |

### 5.2 可信等级

| 级别 | 含义 | 运行时使用 |
|---|---|---|
| K0 原始未核验 | 已保存原文，尚未解析或验证 | 仅浏览和人工取证 |
| K1 已解析 | 已形成切片或结构化抽取 | 可检索，不得作为确定事实 |
| K2 有证据候选 | 有一个或多个来源支持，仍可能冲突 | 可辅助判断，必须显示候选状态 |
| K3 已复核知识 | 经规则校验或人工复核 | 可用于普通任务和建议 |
| K4 受控有效知识 | 已进入知识发布快照并通过相关评测 | 可参与正式 Agent 决策，但不能授予权限 |
| KX 失效/冲突/退役 | 已被覆盖、过期、冲突或撤销 | 默认不召回，只用于审计和反例 |

可信等级不能只由模型置信度决定。

### 5.3 访问密级

| 级别 | 示例 | 检索要求 |
|---|---|---|
| A0 公开 | 已批准公开资料 | 按来源许可使用 |
| A1 内部 | 普通项目资料和操作经验 | 组织身份和项目范围 |
| A2 机密 | 客户、订单、供应、成本和生产资料 | 租户、项目、角色和用途绑定 |
| A3 严格受限 | 工资、个人信息、凭据邻近信息、安全事件 | 最小字段、显式用途和专项权限 |
| A4 禁止进入模型 | 密钥、令牌、原始敏感载荷等 | 只能由确定性代码检查或保存受控引用 |

### 5.4 作用域

- 会话；
- Agent；
- 模块；
- 项目；
- 租户/组织；
- 经额外审批的通用知识。

默认写入和检索最小作用域。跨作用域晋升必须重新脱敏、评测和审批。

## 6. 原文与原始记录保存

### F-KB-001 原文件保存

- 对允许复制的文件保存原始字节、文件名、MIME、大小、修改时间、来源路径和 SHA-256。
- 使用内容寻址存储去重，原始文件一旦摄取不可原地修改。
- 文件更新生成新版本，不覆盖旧版本。

### F-KB-002 Source-only 原文索引

大型、受版权限制、无法复制或必须留在业务系统中的原文，保存：

- 原始路径、URL、系统和对象 ID；
- 文件或记录哈希、大小、版本、修改时间和采集时间；
- 权限、许可、保留策略和可用性；
- 能否全文读取、只能查看元数据还是需要人工授权；
- 最近一次可访问检查结果。

Source-only 不是摘要，它是指向原始证据的受控索引。

### F-KB-003 数据库和 API 原始快照

- 对需要复核的数据库/API 结果保存查询指纹、系统版本、查询时间、主键集合、结果哈希和受控快照引用。
- 高敏感数据可以保存到隔离对象库，由知识层仅保留字段级锚点和访问令牌引用。
- 动态查询结果必须标注新鲜度，不得永久冒充当前状态。

### F-KB-004 对话和工具原始事件

- 保存脱敏后的用户原话、工具输入摘要、完整状态码、输出受控引用和时间顺序。
- 不保存隐藏推理。
- 用户纠正必须保留可复核原话或受控引用，不能只保留系统改写后的总结。

### F-KB-005 归档包内部原文索引

- ZIP、邮件附件包等使用 `archive::entry` 标识具体条目。
- 对每个条目保存独立哈希、大小、类型和父归档关系。
- 不允许仅索引压缩包文件名而丢失内部文件身份。

## 7. 原文锚点

不同格式使用不同锚点：

| 来源 | 原文锚点 |
|---|---|
| Markdown/TXT/源码 | 行号、字符范围、标题路径 |
| PDF | 页码、文本框坐标、段落或图表编号 |
| DOCX | 标题路径、段落 ID、表格/行/单元格 |
| XLSX/CSV | 工作表、表名、行号、列名、单元格范围 |
| 图片/OCR | 页/图 ID、区域坐标、OCR 版本 |
| JSON/YAML/XML | JSONPath/XPath、对象键和数组位置 |
| 数据库 | 数据源、schema、表、主键、版本/时间 |
| API | 服务、接口、请求指纹、响应对象 ID、时间 |
| 网页 | 原始 URL、抓取时间、标题路径、DOM/文本范围 |
| 工具事件 | TaskID、run_id、step_id、tool_call_id 和事件序号 |
| 对话 | session_id、message_id、角色和时间 |

### F-KB-006 锚点解析

- 所有知识引用通过统一 `SourceRef` 解析器打开原文或受控原始记录。
- 锚点失效时标记 `source_unresolved`，不得静默跳到相似位置。
- 重解析后维护旧锚点到新锚点的迁移关系。

### F-KB-007 原文完整性

- 每次访问原文校验对象版本或哈希。
- 来源变化时，使所有相关派生知识进入待复核状态。
- 文件名、路径或 URL 相同不能代替内容一致性验证。

## 8. 格式感知解析与切片

### F-KB-008 格式感知解析

- PDF 保留页、段落、图、表和阅读顺序。
- DOCX 保留标题层级、段落、列表、表格、批注和修订状态。
- 表格保留工作表、表头、单元格、公式和合并关系。
- 图片保留原图并单独保存 OCR 文本、坐标和识别置信度。
- JSON/XML/YAML 按对象路径切片，不按固定字符强切。
- 日志按事件时间、请求 ID、错误堆栈和会话分组。
- 对话按消息和任务步骤保存，不将多个角色混成单一摘要。

### F-KB-009 原文切片

- 每个切片保存原文，不只保存摘要或向量。
- 切片之间保留父子、前后、标题和跨页关系。
- 表格、代码、命令和错误堆栈不得被不合理截断。
- 超大对象允许只保存索引和按需读取能力，但必须能回到原文。

### F-KB-010 解析失败队列

- 保存原文件和失败原因。
- 支持重新解析、OCR、专用工具、人工标注或仅元数据索引。
- 解析失败不得标记为“无有效内容”。

## 9. 分类存储

### 9.1 业务数据知识

按模块分类：

- M0：文档、主数据、BOM、SOP、工图和来源血缘；
- M1：客户订单、产品明细、料号和生产订单映射；
- M2：BOM/SOP 版本、审批、路线和工程规则；
- M3：需求、库存、预占、在途和缺口；
- M4：采购单、供应商、邮件、承诺交期和到货；
- M7：送货、质检、库存归属、领料和超领；
- M5：PMC 输入、约束、资源、WIP、计划、版本和结果。

### 9.2 操作和故障知识

按以下分类保存：

- 工具使用；
- 数据导入和导出；
- 部署和回退；
- 权限和身份；
- 环境、依赖和网络；
- 测试、浏览器和人工验收；
- 错误签名、根因、无效尝试和恢复步骤；
- 停止条件和禁止操作。

### 9.3 Agent 和工作流知识

- Agent 职责、工具能力和边界；
- Agent 指令和 Prompt 版本；
- 工作流节点、状态、条件、Gate 和回退；
- 工具输入输出契约；
- 参数、模板、检查表和回复规范；
- 自进化候选、审批、影子、灰度和生产版本。

### 9.4 决策与证据知识

- 用户原始决定及作用域；
- 决策解释、影响对象和复审条件；
- 证据、反证、纠正、冲突和替代关系；
- 评测、验收、失败和未验证边界。

## 10. 摄取与发布状态机

```text
discovered
  → fingerprinted
  → stored_or_referenced
  → parsed
  → segmented
  → classified
  → indexed
  → validated
  → available
```

派生知识另行经过：

```text
extracted
  → candidate
  → deduplicated
  → conflict_checked
  → evidence_ready
  → reviewed
  → released
```

异常状态包括：

```text
access_blocked / parse_failed / source_missing /
needs_ocr / needs_human / conflict / stale /
invalidated / retired / deleted
```

### F-KB-011 摄取预览

- 摄取前展示来源、数量、大小、类型、权限、敏感性、复制或 source-only 方式和预计索引范围。
- 未经授权不得扫描额外目录、数据库或外部服务。

### F-KB-012 幂等摄取

- 同一来源版本重复摄取不创建重复对象。
- 同内容多来源合并内容对象但保留多个 SourceRef。
- 摄取中断后可从检查点恢复。

### F-KB-013 索引发布快照

- 原文摄取完成不代表立即供所有 Agent 使用。
- 按项目、租户、Agent 和知识可信等级生成不可变索引发布快照。
- 保存当前测试、灰度和生产知识快照指针。

## 11. 多索引体系

### F-KB-014 原文目录索引

所有原始来源，包括未解析和 source-only 资产，都必须能按以下字段检索：

- 来源名称、路径、URL、系统和对象 ID；
- 文件类型、大小、哈希、时间和版本；
- 项目、租户、模块、产品、订单和业务对象；
- 可访问性、解析状态、权限和保留策略。

### F-KB-015 全文索引

- 索引原文切片，而不是只索引摘要。
- 支持精确短语、编号、字段名、料号、错误码和中文分词。
- 摘要和标题可以参与召回，但必须标记为导航字段。

### F-KB-016 向量索引

- 对原文切片、案例、经验和规则分别建立向量，不把全部类型混入同一无分类空间。
- 向量记录必须指向具体原文切片或知识对象。
- 嵌入模型变化时建立新索引版本，不覆盖旧索引。

### F-KB-017 结构化索引

- 支持字段、实体、状态、版本、时间、可信等级、权限和作用域过滤。
- 业务事实优先通过结构化条件检索，再使用语义检索扩展。

### F-KB-018 关系图索引

至少支持：

- 来源文档 → 切片 → 事实/经验；
- 客户订单 → 产品明细 → 生产订单；
- 生产订单 → BOM → SOP/路线 → 物料；
- 物料 → 库存/在途/采购/到货；
- 路线 → 工位/人员/机器/模治具/WIP → PMC；
- 错误 → 工具调用 → 根因 → 修复 → 回归用例；
- 决定 → 影响对象 → 替代决定；
- 提案 → 证据 → 评测 → 审批 → 版本 → 回退。

### F-KB-019 错误与操作序列索引

- 按错误码、异常类型、消息模板、工具、步骤、环境和版本索引。
- 保存操作序列的前置状态、工具顺序、参数类别、结果和停止点。
- 支持“相同错误不同根因”和“不同错误同一根因”的关系。

## 12. 分级检索

检索深度与知识可信等级相互独立。系统按需求逐级加深，不默认一次加载全部知识。

### Q0 作用域和权限过滤

在任何召回之前固定：

- 当前用户、租户、项目、Agent 和任务；
- 允许的数据密级和知识快照；
- 当前环境、模块和版本；
- 是否允许访问原文、外部系统或敏感快照。

Q0 不通过时直接拒绝或返回无权限，不允许先召回后过滤。

### Q1 精确检索

优先处理：

- ID、文件名、路径、URL、哈希；
- 订单号、料号、BOM/SOP 编号、TaskID；
- 错误码、异常文本、工具名和版本；
- 数据库主键和 API 对象 ID。

精确命中可以提前缩小后续搜索空间。

### Q2 分类与元数据检索

- 按业务模块、知识类型、可信等级、来源、时间和状态过滤。
- 优先选取当前环境、当前版本和当前业务对象的知识。
- 可用于列出“所有相关原文”，即使内容尚未成功解析。

### Q3 原文全文与切片检索

- 使用关键词、短语、字段名和上下文窗口召回原文切片。
- 返回命中片段及其前后文、标题路径和原文锚点。
- 表格命中必须返回列名、行键和单元格范围。

### Q4 语义和案例检索

- 对自然语言问题、相似异常和不同表述使用向量召回。
- 同时检索成功案例、失败案例、反例和操作经验。
- 语义相似但版本、环境或业务对象不匹配时降低排序。

### Q5 关系与影响检索

- 沿实体图、血缘和影响关系扩展。
- 用于查找一个纠正会影响哪些订单、数据、规则、流程、评测和版本。
- 对关系边保存来源，禁止出现没有证据的“推测关系”进入正式图谱。

### Q6 原文展开与证据核对

在以下场景必须进入 Q6：

- 高风险业务判断；
- 用户要求查看依据或原文；
- 多来源冲突；
- 摘要与结构化记录不一致；
- 准备修改 Agent 指令、工作流或生产配置；
- 准备正式审批、晋升或发布。

Q6 读取原文件、原始记录或受控 source-only 来源，确认哈希和锚点后再形成结论。

## 13. 查询路由

### F-KB-020 查询类型识别

系统识别但不只依赖关键词：

- 查原文；
- 查事实；
- 查字段或实体映射；
- 查相似错误；
- 查操作步骤；
- 查业务规则；
- 查历史决定；
- 查影响范围；
- 查 Agent/工作流版本；
- 查证据和验收结果。

### F-KB-021 自适应检索计划

- 模型可以选择查询词、组合索引和是否继续深挖。
- 确定性系统执行权限过滤、预算、最大深度、来源完整性和停止门。
- 达到足够证据时停止；证据不足、冲突或无权限时返回明确状态。

### F-KB-022 分类召回配额

- 查询相似问题时分别从原文、事实、经验、反例和规则中取候选。
- 防止某一类大量摘要或相似案例淹没原始证据。
- 高风险任务必须至少保留一个原文证据通道。

### F-KB-023 重排与证据充分性

排序综合：

- 精确匹配；
- 业务实体和环境一致性；
- 可信等级；
- 来源权威度和新鲜度；
- 语义相关性；
- 成功/失败使用记录；
- 冲突、过期和反例惩罚。

模型分数不能覆盖权限、可信等级和来源状态。

## 14. 检索结果和上下文包

### F-KB-024 结果对象

每条结果至少包含：

- 结果类型和知识 ID；
- 标题或简短导航摘要；
- 原文命中片段；
- SourceRef 和可打开的原文锚点；
- 来源类型、时间、版本、可信等级和访问范围；
- 适用条件、不适用条件和冲突状态；
- 该结果支持的具体回答、操作或判断。

### F-KB-025 上下文装配

- 上下文包以少量相关原文片段和结构化事实为主。
- 摘要用于说明为什么召回，不替代原文片段。
- 相关经验必须同时提供适用条件和反例。
- 超出上下文预算时优先保留权威原文、当前事实和直接反例。

### F-KB-026 引用和原文打开

- Agent 回答中的关键事实、规则和建议可展开到具体原文。
- 用户可从回答直接打开页、段、表格、单元格、记录或工具事件。
- Source-only 来源无法自动打开时，显示受控位置、访问条件和最近可用状态。
- 原文不可访问时标记 `source_unavailable`，不得假装已核对。

## 15. 摘要的正确定位

摘要只允许用于：

- 导航；
- 主题聚类；
- 快速预览；
- 检索结果解释；
- 上下文预算不足时的候选筛选。

摘要禁止用于：

- 替代原文保存；
- 独立证明高风险业务事实；
- 隐藏相反证据、异常、未解析表格或附件；
- 覆盖用户纠正后的原始内容；
- 作为 `Agent.md`、工作流或生产配置修改的唯一证据。

### F-KB-027 摘要完整性门

- 摘要对象必须列出全部 SourceRef。
- 摘要必须区分事实、推断、未知和冲突。
- 原文删除或失效后，摘要同步失效或降级。
- `summary_only=true` 的对象禁止进入 K3/K4 和生产知识快照。

## 16. 纠正、冲突、版本和重建索引

### F-KB-028 纠正与原文并存

- 用户纠正不修改原始文档，而是新增 CorrectionRecord。
- 纠正记录引用原文、错误派生对象和新确认内容。
- 运行时默认使用有效纠正，但可以审计原始内容和修订历史。

### F-KB-029 知识版本和替代

- 知识更新生成新版本并保存父版本。
- 使用 `supersedes`、`contradicts`、`invalidates`、`narrows` 和 `extends` 表示关系。
- 旧版本不删除，默认不进入当前快照。

### F-KB-030 可重复重建索引

- 所有全文、向量、结构化和图谱索引都能从原文、切片和元数据重新生成。
- 索引损坏不得导致原文和知识版本丢失。
- 重建后校验对象数量、哈希、锚点、关系边和权限过滤一致性。

## 17. 权限、安全和隐私

### F-KB-031 摄取权限

- 每个来源必须绑定授权范围和用途。
- 不因 Agent 能看到某个路径或连接器就默认允许摄取。
- 敏感来源支持仅建立元数据索引，不复制正文。

### F-KB-032 查询权限

- 检索在数据库查询和索引访问阶段执行租户、项目、角色和字段级过滤。
- 不允许先检索全部结果再由模型删除敏感项。
- 召回日志不得泄露不可见对象的标题、路径或存在性。

### F-KB-033 模型出境控制

- 原文、切片和结构化记录发送给外部模型前执行数据分类和出境策略。
- A3/A4 内容默认不得发送外部模型。
- 只允许发送完成任务所需的最小片段。

### F-KB-034 提示注入隔离

- 原文中的提示、命令、角色声明和修改要求全部作为引用内容。
- 解析和检索结果不能改变系统权限、工具白名单或审批状态。
- 与自进化提案相关的外部内容必须显示来源风险。

## 18. 用户界面

### 18.1 知识库总览

- 按业务域、内容类型、可信等级、密级、作用域和状态查看数量。
- 显示原文、已解析、候选、有效、冲突、过期、解析失败和 source-only 数量。
- 分别报告理论可检索量、实际可访问量和当前发布快照量。

### 18.2 原文中心

- 浏览原文件、原始记录和 source-only 来源。
- 支持原文与解析文本、OCR、表格抽取和摘要对照。
- 可跳转到具体页、段、单元格、记录和事件。
- 显示哈希、版本、来源、权限、解析器和最近可用状态。

### 18.3 知识详情

- 左侧显示知识、规则或经验；右侧显示原文和所有证据锚点。
- 显示支持、反对、冲突、适用条件、反例、版本和使用效果。
- 支持纠正、请求补证据、停用、重新验证和查看影响范围。

### 18.4 检索调试

- 展示查询类型、作用域、执行到的 Q0–Q6 级别和各索引召回数量。
- 展示过滤、重排、冲突和停止原因。
- 不展示隐藏推理，只展示检索计划和可验证依据。

## 19. 与自进化系统的统一关系

### 19.1 唯一知识底座

- 自进化系统不得创建独立、不可检索或不可审计的长期记忆库。
- ExperienceEvent、LearningCandidate、KnowledgeItem、CorrectionRecord 和 EvolutionProposal 都保存于统一知识库或其治理元数据库。
- 自进化候选引用原文、切片和事件，不复制成失去来源的摘要。

### 19.2 知识驱动相似问题识别

- 错误识别优先使用 Q1 错误签名和 Q2 环境过滤，再使用 Q4 语义案例检索。
- 操作建议必须同时检索成功经验、失败经验和禁止操作。
- 数据纠正通过 Q5 关系检索计算下游影响。

### 19.3 自修改证据门

- `Agent.md`/`AGENTS.md` 和工作流提案至少引用一个可打开原文锚点和一个真实任务/评测事件。
- 仅由摘要、模型自评或单次无后置条件成功产生的提案不能进入审批。
- 审批、影子、灰度和生产版本都绑定精确知识快照 ID。

## 20. 核心接口能力

接口名称可以按现有架构调整，但必须具备以下能力：

- 来源发现和摄取预览；
- 原文件上传或 source-only 登记；
- 数据库/API 快照登记；
- 解析、切片、OCR 和重新解析；
- 分类、实体和关系写入；
- 原文、全文、向量、结构化、图谱和操作序列检索；
- 原文锚点解析和受控打开；
- 候选、纠正、冲突和知识版本管理；
- 索引快照创建、灰度、晋升和回退；
- 查看、导出、停用、删除、重新验证和影响分析；
- 索引完整性、权限和检索质量评测。

## 21. 验收标准

### 21.1 原文保留

| 验收项 | 门槛 |
|---|---|
| 原文件或 source-only 来源登记率 | 100% |
| 派生切片可回到原文锚点 | 100% |
| K3/K4 知识具有可解析 SourceRef | 100% |
| `summary_only` 对象进入 K3/K4 | 0 |
| 同内容多来源丢失来源路径 | 0 |
| 同名异内容被错误合并 | 0 |
| 未解析文件从原文目录消失 | 0 |
| 归档包条目缺少 `archive::entry` 身份 | 0 |

### 21.2 索引完整性

- 原文对象、切片、结构化对象和图节点无悬空引用。
- 内容哈希重复对象为 0，但来源引用允许多条。
- 重建索引后对象数量、权限结果和 SourceRef 解析结果一致。
- 解析失败、OCR 低置信和 source-only 状态可检索并可统计。

### 21.3 分级检索

| 任务 | 首版建议门槛 |
|---|---|
| 精确 ID/编号/哈希查询 | Top-1 准确率 100% |
| 原文片段检索 | Recall@5 ≥ 95% |
| 相似错误和操作经验 | Recall@5 ≥ 90% |
| 结构化业务事实 | 当前版本和实体过滤准确率 100% |
| 原文锚点打开 | 成功率 100%，失效时必须明确报错 |
| 高风险回答原文展开 | 覆盖率 100% |
| 冲突知识自动当作单一事实使用 | 0 |
| 跨租户或越权召回 | 0 |

### 21.4 摘要负向验收

必须验证：

- 删除摘要后仍能从原文和结构化索引检索；
- 只有摘要、缺少原文引用的对象无法晋升；
- 摘要遗漏表格、附件或反证时，高风险检索会进入原文展开；
- 原文版本变化后旧摘要自动失效；
- Agent 无法用自己生成的摘要为自己的配置修改提供唯一证据。

### 21.5 安全和恢复

- 权限过滤、提示注入、敏感字段、模型出境和删除测试通过。
- 摄取、解析、索引、发布和回退均支持中断恢复和幂等。
- 当前知识快照可以原子回退，原文层不受影响。
- 索引全损后可从原文、切片和元数据完整重建。

## 22. 分阶段实施

### P0：来源和原文底座

- SourceArtifact、SourceRef、SourceSegment、权限、CAS 和 source-only 目录。
- 文件、数据库/API、工具事件和对话的最小摄取链。
- 原文查看、锚点解析、哈希和完整性验证。

### P1：分类、解析和基础检索

- 格式感知解析、切片、分类和解析失败队列。
- 原文目录、全文和结构化索引。
- Q0–Q3 检索和原文引用。

### P2：语义、图谱和操作检索

- 向量、关系图、错误签名和操作序列索引。
- Q4–Q6 检索、混合重排和上下文包。

### P3：知识治理

- 候选、纠正、冲突、可信等级、版本和发布快照。
- 重新索引、失效传播、查看、导出、停用和删除。

### P4：自进化统一接入

- 自进化事件、经验、评测和提案全部接入统一知识库。
- Agent/工作流修改审批绑定知识快照和原文证据。
- 影子、灰度、晋升和回退使用同一索引发布机制。

## 23. 发布判定

- 原文保存和 SourceRef 链未完成：`blocked`；
- 只有摘要、向量或知识图谱，没有原文：`blocked`；
- Q0 权限过滤未在检索前执行：`blocked`；
- 原文/全文/结构化检索可用但语义和图谱尚未完成：`limited`；
- Q0–Q6、原文引用、知识治理、索引重建和安全验收通过：知识库可判定 `ready`；
- 自进化是否 `ready` 仍需单独通过提案、审批、影子、灰度和回退验收。
